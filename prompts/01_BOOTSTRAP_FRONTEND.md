/goal Bootstrap only the frontend foundation for ORI Learning.

Read the project docs and workspace rule first.

Requirements:
- React + TypeScript + Vite.
- Tailwind CSS.
- React Router.
- Clean feature-based folder structure.
- Routes for login, dashboard, vocabulary, grammar, listening, reading, account, expired and admin.
- Use placeholder pages only.
- Create a reusable app shell with responsive mobile navigation.
- ORI visual direction: bright blue + white, clean, professional, readable on phones.
- No Supabase connection yet.
- No mock authentication architecture beyond placeholders.
- Avoid unnecessary dependencies.

Verification:
- install dependencies
- run typecheck if configured
- run production build
- use browser to verify main routes if available

At the end, report:
1. files created/changed
2. commands run
3. routes available
4. any unresolved issue

Do not proceed to Supabase or business logic.
